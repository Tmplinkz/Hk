# Telegram Bot API
aiogram==3.30.0
python-dotenv

# Media / metadata
hachoir
beautifulsoup4==4.9.1

# Downloads
pySmartDL
requests

# Database
dnspython==2.3.0
motor

# Other runtime dependencies
psutil
cloudscraper
lk21
lxml
httpx
speedtest-cli

# RSS / Scheduler
aiohttp
anitopy

# {audio} template variable – resolves ffprobe language codes (e.g. "jpn") to display names
langcodes

# Torrent / magnet downloads
# aria2c is a system binary – installed via apt/apk in Dockerfile, NOT pip

# MTProto Telegram media transfer
wzgram[fast]
